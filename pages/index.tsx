import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";

const Home: NextPage = () => {
  return (
    <div className="">
      <Head>
        <title>Khairi | Airbnb clone with nextjs and tailwindcss</title>
        <link rel="icon" href="/favicon.ico" />
      </Head>

      {/** Header */}
      {/** Banner */}
    </div>
  );
};

export default Home;
